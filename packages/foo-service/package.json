{
  "name": "@poc/foo-service",
  "version": "1.0.1",
  "dependencies": {
    "@grpc/grpc-js": "^1.8.10",
    "@grpc/proto-loader": "^0.7.5"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "nodemon",
    "start": "node build/index.js",
    "generate": "buf generate --template=protobuf/buf.gen.yaml"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.14.0-1",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  }
}
